package timekeeping.my.controller.features.validate;

/**
 * The validation provides enum of field able to validate
 * */
public enum Validation {
    LOGIN, PASSWORD, EMAIL, PHONE, ENTER_TIME, ACTIVITY_NAME, DESCRIPTION
}
